node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Local playground provider configuration — may hold credentials, never committed.
/.precall-playground/
